# 🚀 GitHub Pages 배포 가이드 (완전 초보자용)

이 문서대로만 따라 하면 `https://lemonnn2.github.io/mfgip_tf/` 주소로 지도 앱이 배포됩니다.
파일을 GitHub에 올리기만 하면 **자동 배포 + 히스토리 관리**가 동시에 됩니다.

---

## 📦 올려야 할 파일 (총 6개)

```
mfgip_tf/
├── index.html          ← 메인 페이지
├── map-data.json       ← 기본 매장 데이터
├── README.md           ← 설명 문서
├── .nojekyll           ← (빈 파일) 배포 안정용
├── css/
│    └── style.css      ← 디자인
└── js/
     └── app.js         ← 기능
```

⚠️ **폴더 구조가 꼭 유지돼야 합니다.**
- `style.css` 는 반드시 `css/` 폴더 안
- `app.js` 는 반드시 `js/` 폴더 안

---

## 1단계. 파일을 내 컴퓨터로 받기

이 Genspark 프로젝트 화면에서 각 파일을 다운로드하거나, 소스 내려받기(Export/Download) 기능을 사용해 위 6개 파일을 컴퓨터의 한 폴더(예: 바탕화면의 `mfgip_tf` 폴더)에 저장하세요.
- 폴더 구조 그대로 저장하는 것이 가장 편합니다.

---

## 2단계. GitHub 웹사이트에서 업로드

1. 브라우저에서 접속 → **https://github.com/lemonnn2/mfgip_tf**
2. (로그인 안 돼 있으면 로그인)
3. 화면 오른쪽 위 **`Add file`** 버튼 클릭 → **`Upload files`** 선택
4. 받아둔 파일/폴더를 화면 안으로 **드래그 앤 드롭**
   - `css` 폴더와 `js` 폴더는 폴더째로 끌어다 놓으면 구조가 유지됩니다.
   - `.nojekyll` 파일도 잊지 말고 함께 올리세요.
5. 화면 아래 **`Commit changes`** (초록 버튼) 클릭
   - 위 칸에 간단한 메모를 적어도 됩니다. 예: `지도 앱 첫 배포`
6. 끝! 이 순간이 바로 "커밋" = **히스토리 한 칸이 기록**된 것입니다.

---

## 3단계. GitHub Pages 켜기 (최초 1회만)

이미 예전에 `lemonnn2.github.io/mfgip_tf`를 쓰고 있었다면 이 단계는 이미 켜져 있을 수 있습니다. 확인해 보세요.

1. `https://github.com/lemonnn2/mfgip_tf` 에서 상단 **`Settings`** 클릭
2. 왼쪽 메뉴에서 **`Pages`** 클릭
3. **Source** 를 `Deploy from a branch` 로 두고
   - Branch: **`main`** , 폴더: **`/ (root)`** 선택 → **Save**
4. 잠시(1~2분) 기다리면 위쪽에 배포 주소가 뜹니다:
   👉 **https://lemonnn2.github.io/mfgip_tf/**

---

## 4단계. ⭐ 카카오 지도 도메인 등록 (이거 안 하면 지도가 회색!)

배포 주소가 나왔으면 그 주소를 카카오에 등록해야 지도가 보입니다.

1. https://developers.kakao.com 로그인
2. **내 애플리케이션** → 우리 앱 선택
3. 왼쪽 **앱 설정 → 플랫폼** → **Web 플랫폼 등록/수정**
4. **사이트 도메인**에 아래 주소를 추가하고 저장:
   ```
   https://lemonnn2.github.io
   ```
   (경로 `/mfgip_tf` 없이 `https://lemonnn2.github.io` 까지만 넣으면 됩니다)
5. 저장 후 페이지 새로고침 → 지도가 정상 표시됩니다. 🎉

---

## 🔁 앞으로 수정할 때 (히스토리 관리)

1. Genspark에서 원하는 수정 요청 → 파일이 바뀜
2. 바뀐 파일을 다시 컴퓨터로 받기
3. GitHub에서 **`Add file → Upload files`** 로 같은 파일을 다시 올리고 **`Commit changes`**
   - 같은 이름 파일은 **덮어쓰기(업데이트)** 됩니다.
4. 커밋할 때마다 GitHub가 **변경 이력(누가·언제·무엇을 바꿨는지)** 을 자동으로 남겨줍니다.
   - 이력은 저장소 상단 **`commits`** (시계 모양) 에서 볼 수 있고, 언제든 예전 버전으로 되돌릴 수 있습니다.

---

## ❓ 자주 나는 문제

| 증상 | 원인 / 해결 |
|---|---|
| 지도가 회색으로만 나옴 | 4단계(카카오 도메인 등록) 안 함 → 등록 후 새로고침 |
| CSS/기능이 안 먹음 | `css/`, `js/` 폴더 구조가 깨짐 → 폴더 안에 정확히 있는지 확인 |
| 페이지가 404 | Pages가 아직 안 켜졌거나 배포 대기 중 → 3단계 확인, 1~2분 대기 |
| 업로드한 매장 데이터가 안 보임 | 그 데이터는 **관리자가 올린 브라우저에만** 저장됨(localStorage). 모두에게 공유하려면 `map-data.json` 파일 자체를 교체해서 커밋해야 함 |
